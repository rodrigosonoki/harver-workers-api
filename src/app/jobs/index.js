const RegistrationMail = require("./RegistrationMail");
const ForgotPasswordMail = require("./ForgotPasswordMail");

const jobs = {
  RegistrationMail,
  ForgotPasswordMail,
};

module.exports = jobs;
